{
  "name": "repomind-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "lint": "eslint . --ext .js",
    "test": "vitest run",
    "build": "node --check index.js && node --check routes/auth.js && node --check routes/jobs.js && node --check routes/settings.js && node --check routes/assistant.js && node --check models/User.js && node --check models/Job.js && node --check middleware/auth.js && node --check middleware/validate.js && node --check middleware/errorHandler.js && node --check utils/crypto.js"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "mongodb-memory-server": "^11.2.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0",
    "vitest": "^1.6.1"
  }
}
